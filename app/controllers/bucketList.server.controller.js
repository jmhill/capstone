var BucketList = require('mongoose').model('BucketList');

exports.addPlace = function() {
	// add a new place/destination to the bucketlist
};

exports.list = function() {
	// list out the different place objects in the bucketlist
};